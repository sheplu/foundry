# @foundry/elements

Native web components for the foundry design system. Every component extends the shared `FoundryElement` base, renders via cloned templates into an open shadow root, and is styled exclusively through `--foundry-*` tokens. Depends on `@foundry/tokens`.
